before:
  hooks:
    - go mod tidy
    - sh scripts/generate-go-version.sh {{ .Version }}
project_name: kkrtctl
snapshot:
  version_template: "{{ .Tag }}-next"
changelog:
  sort: asc
  filters:
    exclude:
      - '^test:'
      - '^docs:'